# Lessons

- When extending platform support, preserve the current Windows path as-is and add new OS-specific entry points alongside it instead of replacing it.
- When a user asks "what exactly do I need to pass on Mac", answer with the concrete folder and launcher names, not just generic platform compatibility notes.
